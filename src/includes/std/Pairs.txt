# Define the Church pair
pair a b := λ f . f a b
first p := p (λa b. a)
second p := p (λa b. b)

# Test implementation of pair, first, and second
:assert first (pair x y) ≡ x
:assert second (pair x y) ≡ y
