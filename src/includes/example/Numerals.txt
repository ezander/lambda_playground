# Make a signed numeral example here
# Use boolean, numerals and pair
# define Nat, Signed, define 0', 1', 2', ... -1', -2, -3'
# define +' and -', >'
#

# Church numerals
zero      := λf x. x
succ  n f x := f (n f x)
plus  m n f x := m f (n f x)
mult  m n f   := m (n f)
one   := succ zero
two   := succ one
three := succ two
four  := succ three
five  := succ four

plus two three
