# Example for pred using pairs
# Example for pairs in pred
# What about triple, first/3, second/3
# What about Lists, put numberals in list, map them by sqr, add them (filter?)

# Church pairs
pair  a b f := f a b
fst   p     := p (λa b. a)
snd   p     := p (λa b. b)

# Church booleans (needed by fst/snd)
true    := λx y. x
false   := λx y. y

snd (pair false true)
